source = "system"
directory = "/usr/lib64"

# source = "mesa"
# directory = "usr/lib"
# directlyDependentSharedLibraries = [
#   "libdrm.so.2",
#   "libexpat.so.1",
#   "libm.so.6",
#   "libc.so.6",
# ]
